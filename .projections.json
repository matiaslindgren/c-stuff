{
  "*.c": {
    "template": [
      "int main(int argc, char* const argv[argc + 1]) {",
      "  return 0;",
      "}"
    ]
  },
  "*.h": {
    "template": [
      "#ifndef {basename|uppercase}_H_INCLUDED",
      "#define {basename|uppercase}_H_INCLUDED",
      "",
      "#endif  // {basename|uppercase}_H_INCLUDED"
    ]
  }
}
